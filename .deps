#! /bin/bash
set -e

set -o allexport
. ./app/profile.conf
. ./app/app.conf
set +o allexport

type -p ssh-keygen >/dev/null ||
  ./.deps.d/install-ssh

[ -f "${HOME}/.profiledir" ] ||
  ./.deps.d/install-profiledir
